# 班主任工作台 · 设计思路 v0.3

> 配套文件：**《README.md》**（使用说明）、**《UI草图.html》**（线框稿）
> 日期：2026-09-11 ｜ v0.3 更新：**第一期 MVP 已编码完成并通过端到端测试**（Python FastAPI + 原生 JS 前端，代码在 `app\`，启动见「启动工作台.bat」）
> 历史：v0.2 按班主任反馈重排首页；已确认决策见第 7 节

---

## 1. 产品定位

给高职班主任用的**本地单机、AI 驱动**的工作台，五条设计原则：

1. **数据不出本机** —— 所有学生信息只存在 `C:\AI Agent生成文件\班主任工作台` 专用文件夹内，不上传任何云端；AI 只在调用大模型 API 时把"当次对话内容"发出去，落库全在本地。
2. **对话即录入** —— 一个对话框是核心入口，大部分信息靠打字告诉 AI，AI 理解后写入对应模块；表格页保留手工增改作为补充。
3. **一次录入、处处联动** —— 请假 → 自动进考勤 → 首页"本周到校"变化；成绩录入 → 自动触发学业预警 → 推送首页红黄灯；谈心/家访 → 自动进一生一档时间线。
4. **界面从简** —— 固定四块结构：顶部日程条 + 左侧导航 + 中间内容区 + 右侧 AI 对话栏，不随模块变化。
5. **可持续迭代** —— 数据全部 JSON 化、模块之间通过联动规则解耦，功能随时可加不改旧数据。

---

## 2. 技术方案（推荐）

| 方案 | 说明 | 评价 |
|---|---|---|
| **A. 本地 Web 应用（推荐）** | Python (FastAPI) 起本地服务，浏览器访问 localhost，数据为 JSON 文件 | 开发快、库全、迭代最容易 |
| B. 桌面应用 Electron / Tauri | 打包成 exe 双击运行 | 更像"软件"，但打包调试成本高，适合后期再套壳 |
| C. 纯前端单 HTML + localStorage | 无后端 | 最轻，但 Excel/Word/图片管理受限，数据量大了不可靠 |

**推荐 A**。理由：AI 调 API、Excel/Word 读写、图片与备份管理都需要一个本地后端；浏览器界面跨窗口好用；以后想打包成桌面软件，直接套 Tauri 壳即可，数据结构不用动。

技术栈清单：

- 后端：Python 3.11 + FastAPI
- 办公文档：`openpyxl`（Excel 读写）、`python-docx`（Word 读写生成）
- 前端：原生 HTML/CSS/JS 起步（界面复杂了再上 Vue3 + Element Plus）；图表用 ECharts
- AI：默认 DeepSeek（班主任已有 API Key）；预留 OpenAI 兼容格式接口，base_url / model / key 存本地 `config.json`，可切换其他厂商
- AI 调用方式：让大模型按**约定的 JSON Schema 输出结构化指令**（意图 + 字段），后端校验后写库；写操作默认需确认

---

## 3. 数据架构（本地文件数据库）

### 3.1 目录结构

```
C:\AI Agent生成文件\班主任工作台\
├─ app\                      程序代码（与数据分离）
├─ data\                     ← 全部业务数据（JSON）
│  ├─ students.json          花名册主档（学号 student_id 为全局主键）
│  ├─ tags.json              标签体系（留守/单亲/学困/退役士兵/建档立卡…自定义）
│  ├─ todos.json             待办与日程（首页看板 + 顶部日程条共用一份数据）
│  ├─ attendance\            考勤，按月分文件（2026-09.json）
│  ├─ leaves.json            请假与审批
│  ├─ grades\                成绩，按考试分文件（含实训、考证成绩）
│  ├─ certificates.json      技能考证（1+X、职业资格）
│  ├─ competitions.json      技能竞赛（报名、获奖、积分）
│  ├─ talks.json             谈心谈话
│  ├─ family_contacts.json   家校沟通台账
│  ├─ visits.json            家访计划与记录
│  ├─ notices.json           通知发布与家长已读统计
│  ├─ mental.json            心理预警与干预跟踪
│  ├─ internships.json       实习登记 / 过程跟踪 / 巡查记录
│  ├─ employment.json        就业意向 / 签约状态 / 毕业去向
│  ├─ class_events.json      班级活动 + 量化考核
│  ├─ meetings.json          主题班会（计划、记录、教案路径）
│  ├─ worklog.json           工作日志 / 周志
│  ├─ plans.json             学期计划总结、奖助评定、备赛等计划
│  ├─ warnings.json          预警（规则引擎自动生成，红/黄两级）
│  └─ ops_log.json           操作日志（AI 与人工每次写入留痕，支持撤销）
├─ files\                    交换文件区
│  ├─ excel\                 导入的花名册/课表/成绩单，导出的各种表
│  ├─ word\                  导出的报告、教案、通知
│  ├─ photos\                班会/活动/请假条等图片（按模块分子目录）
│  └─ backups\               每次写入前的自动快照（按日期归档）
└─ config.json               班级信息、API Key、学期周历、预警阈值等配置
```

### 3.2 关键机制

- **以学号为主键**：所有模块记录都挂 `student_id`，学生信息修改/异动只改主档，各模块自动跟随。
- **自动保存三步曲**：任何写入 = ① 旧文件快照进 `backups\` → ② 写 JSON → ③ 记 `ops_log.json`。断电/崩溃不丢数据，且每次变更可撤销。
- **联动规则表**（"一次录入、多模块复用"的实现方式），例如：
  - 请假审批通过 → `leaves.json` + 当月考勤标"假" + 首页"本周到校"刷新 + 可选生成家长通知草稿
  - 成绩录入完成 → 重算及格率/优秀率 → 触发学业预警规则 → `warnings.json` + 首页红黄灯
  - 谈心记录 → `talks.json` + 该生成长档案时间线追加 + 关联"后续跟进"生成待办
- **输出**：任何数据可导出 JSON / Excel / Word；学期末学情分析报告由 AI 汇总各模块生成 Word 初稿。
- **图片**：请假条、活动照片存 `files\photos\`，JSON 里只记路径，避免数据文件膨胀。

### 3.3 首次使用设置向导（onboarding）

第一次启动时按步骤引导（此后可随时在「班级设置」修改，首页常驻蓝色提醒条）：

1. **开班信息**：班级名称、年级、专业、班级人数（界面提示"通常 40 人以下"，超出给确认弹窗）
2. **班主任本人信息**：姓名、电话（通知落款与家长回访用）
3. **AI 配置**：默认填 DeepSeek base_url 与 API Key 输入框，并注明"Key 仅存本机 config.json"；选择"厂商：DeepSeek / 其他(OpenAI 兼容)"
4. **数据准备**：引导导入花名册 Excel（或逐条录入）→ 全学期课表 Excel
5. 向导完成进入首页；未完成可跳过，顶部提示条持续提醒

> 多班规划：班级配置独立保存为 `data/class_config.json`（含 class_id），学生等业务记录均带 class_id 字段，将来扩展多班时旧数据无需迁移。

---

## 4. AI 对话设计（核心入口）

### 4.1 交互闭环

```
用户打字 → 大模型理解 → 输出结构化 JSON（意图 + 字段）
→ 界面回显"写入卡片"（写哪个模块、哪些字段）
→ 确认 / 修改 / 取消
→ 后端写库（快照备份 + 自动保存 + 留痕）
→ 联动模块刷新（首页看板、预警、档案时间线）
→ AI 回执（"已写入 ✔ 考勤已联动"）
```

### 4.2 意图类型清单

| 意图 | 用户示例 | 落点 |
|---|---|---|
| 录入 | "张子豪今天感冒发烧，请假一天" | leaves + attendance + 首页 |
| 批量录入 | "电工基础成绩：张三85 李四76 …"（也支持直接粘贴 Excel 两列） | grades |
| 查询 | "李昊天上学期挂了几门？" | grades → 回显表格 |
| 修改 | "把吴雅婷的宿舍改到 5-306" | students |
| 撤销/纠正 | "刚才那条请假记错了，撤回" | ops_log 回滚 |
| 日程/待办 | "周五下午3点提醒我找刘思远谈心" | todos → 顶部日程条 |
| 生成 | "写一份国庆放假通知" / "给'网络安全'主题班会展个教案" | word / notices / meetings |
| 分析 | "分析这次月考，生成报告" | 成绩分析 + Word 报告 |
| 导入导出 | "把花名册导出成 Excel" / "读一下桌面上的学生花名册.xlsx" | excel |

### 4.3 安全机制

- 写操作先回显确认卡片，确认才落库；删除类操作二次确认。
- 每次写入记 `ops_log.json`，最近操作可一键撤销。
- 重名学生用学号兜底，AI 拿不准就反问，不猜。
- API Key 只存本地 `config.json`；对话中涉及学生的内容仅当次请求发送给所选大模型服务商。

---

## 5. 页面地图（对应草图 12 页）

| # | 页面 | 核心内容 |
|---|---|---|
| 0 | 今日工作台（默认首页） | 顶部蓝色首用提醒条、今日日程信息流（时间轴式，含逾期/明日预告）、预警红黄灯、本周课表、今日到校、待办三栏看板（逾期标红）、快捷入口、底部 AI 主输入框（演示） |
| 1 | 项目说明 | 已确认产品决策表、界面结构、技术方案摘要、页面地图、迭代计划 |
| 2 | 花名册 | 20+ 基本字段、标签筛选、Excel 批量导入导出、学籍异动登记 |
| 3 | 一生一档 | 学生详情 9 个页签（基本信息/学业/考勤/谈心/家校/实习/奖惩/异动/数字画像）+ 成长时间线 |
| 4 | 日常事务 | 请假审批、通知发布（含已读统计）、奖助评定/备赛计划时间轴、月历视图 |
| 5 | 学业成绩 | 成绩录入、分析看板（均分/及格率/柱状图/折线/饼图）、学业预警、考证追踪、竞赛管理、偏科临界 |
| 6 | 实习就业 | 实习登记、周记与企业评价跟踪、下企业巡查、就业意向/签约/毕业去向 |
| 7 | 家校沟通 | 沟通台账、重点学生家访计划、家校协同任务反馈、通知触达统计 |
| 8 | 思政心理 | 心理预警与干预跟踪、谈心谈话、班会计划与 AI 教案、德育活动 |
| 9 | 班级建设 | 班规班约、活动照片墙、量化考核周/月汇总、班委管理 |
| 10 | 统计分析 | 班级画像、学生画像（雷达）、预警看板、一键报表导出、对比分析 |
| 11 | 个人管理 | 工作日志/周志模板、学期计划总结（AI 汇总初稿）、考核指标、培训研修 |
| — | AI 对话与数据流（收于文档页） | 意图清单、写入 JSON 示例、安全机制说明 |

---

## 6. 迭代路线

- **第一期 MVP**：今日工作台 + 花名册 + AI 对话（录入/查询/待办）+ Excel 导入导出 + 自动保存备份 + 顶部日程条。
- **第二期**：请假考勤联动、成绩录入与分析看板、学业预警、谈心/家校沟通台账、通知发布与已读。
- **第三期**：实习就业全流程、心理预警与班会 AI 教案、量化考核、班级/学生画像、Word 报告一键导出。
- **第四期**：学期末学情分析报告全自动生成、多班支持、Tauri 桌面壳打包、（可选）移动端填报。

---

## 7. 已确认的产品决策（班主任 2026-09-03 答复）

| 问题 | 决策 | 设计影响 |
|---|---|---|
| 班级规模 | 当前 1 个班；将来迭代增加多班管理 | 数据模型预留 class_id 字段、班级配置独立成文件（见 3.3），升级不迁移旧数据 |
| 班级人数 / 专业 | 通常 40 人以下；**产品第一次使用需手动输入** | 首次启动向导引导填写并常驻提醒（见 3.3）；示例数据统一按 40 人 |
| AI 大模型 | 用 DeepSeek API（已有 Key） | config.json 存 base_url/key/model；预留其他厂商 OpenAI 兼容接口 |
| 心理测评等数据 | 以 Excel 表格输入 | 「导入 Excel」统一覆盖测评结果、课表、成绩单、花名册：列映射 + 逐条确认 |
| 家长通知渠道 | 不直连微信等通信工具 | 系统生成文本 → 班主任手动复制发群 → 手动/口述回流已读状态 |
| 顶部日程与手机同步 | 暂不做 | 日程仅在本机工作台顶部提醒条与首页日程卡展示（不做 .ics/推送） |

> 以上决策表已同步展示在 UI 草图「项目说明」页，随迭代继续追加，作为编码阶段的需求依据。
